# 개발가이드

`관상가 아솔` 개발하면서 쌓인 메모.

## 로컬 실행

```bash
pip install -r requirements.txt
streamlit run streamlit_app.py
```

`.streamlit/secrets.toml` 에 `LLM_BASE_URL` / `LLM_API_KEY` 가 있어야 한다.
(이 파일은 `.gitignore` 로 제외되므로 깃에 올라가지 않는다.)

## 윈도우에서 개발할 때 주의

콘솔 인코딩이 cp949 라서 `print` 의 이모지가 `UnicodeEncodeError` 를 낸다.
`streamlit_app.py` 최상단에서 stdout/stderr 을 UTF-8 로 고정해 두었으니
**그 블록을 다른 import 아래로 옮기지 말 것.** 먼저 실행돼야 의미가 있다.

## LLM 서버

- OpenAI 호환 엔드포인트. 모델 목록은 `GET {LLM_BASE_URL}/models`.
- **비전 모델만 쓸 수 있다.** 관상은 사진을 봐야 하므로 텍스트 전용 모델은 불가.
- 앞단 Cloudflare 가 약 100초에서 응답을 끊고 **524** 를 준다. 느린 모델은
  그대로 실패하므로, 읽기 타임아웃을 그보다 짧게(90초) 두고 스트리밍으로 받는다.
- 모델 우선순위는 `llm_client.py` 의 `VISION_MODELS` 에서 바꾼다.

## 모듈 캐시 주의

`llm_client.py` 를 고쳤는데 동작이 안 바뀌면 **Streamlit 서버를 재시작**할 것.
파이썬이 import 한 모듈을 캐시하고 있어서, 화면만 새로고침해서는 새 코드가 안 붙는다.

---

## 깃 사용법 (처음 올릴 때)

# 1. 깃 초기화 (이미 했다면 생략 가능하지만 해도 상관없음)
git init
git config --global user.email "you@example.com"
git config --global user.name "Your Name"
git branch -M main

# 2. 방금 만든 GitHub 주소 연결하기 (주소 부분에 복사한 걸 붙여넣으세요!)
git remote add origin https://github.com/본인아이디/저장소이름.git

# 3. 현재 코드들을 장바구니에 담기
git add .

# 4. 포장하기 (이름표 붙이기)
git commit -m "첫 번째 업로드"

# 5. 가지 이름 정리 (오류 방지용)
git branch -M main

# 6. GitHub로 발사!
git push -u origin main 
## 여기서 잠깐 -u 옵션과 -f 옵션의 차이 
### 만약 readme.md와 같은 파일이 있고 없을때 애러 날수 있음 
###  ! [rejected]        main -> main (fetch first)
### error: failed to push some refs to 'https://github.com/EmmettHwang/GwangsangWebAPP.git'

## 이때에 -f 옵션을 사용함. 

### git init에서 한글 문제로 애러 날때 사용
> chcp 65001   
> git init   
> git config --global core.quotepath false
