# 1. 비밀값 - 절대 커밋하지 않는다
# 키는 한 번 올라가면 지워도 이력에 남고, 포크해 간 사람에게도 그대로 복사된다.
# 실제로 옛 Gemini 키 두 개가 그렇게 남아 포크까지 따라갔다(2026-09-03 폐기).
.env
*.env
!*.env.example
.streamlit/secrets.toml

testModel.py
app copy.py

# 3. 파이썬 임시 파일 (쓰레기)
# 파이썬이 실행할 때 만드는 찌꺼기 파일들입니다.
__pycache__/
*.pyc
# 4. 개발 도구 설정
# 내 컴퓨터의 VS Code 설정이라 남에겐 필요 없습니다.
.vscode/
.idea/
.DS_Store

# 임시 작업 폴더
temp/

# 수집한 메일 주소 (개인정보) - 저장소에 넣지 않는다
data/
